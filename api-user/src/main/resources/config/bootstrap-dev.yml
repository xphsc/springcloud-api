spring:
  application:
    name: user
  cloud:
    config:
      uri: http://localhost:8888
      profile: dev
encrypt:
  failOnError: false
management:
  security:
    enabled: false
endpoints:
  health:
    sensitive: false
  trace:
    enabled: true
    sensitive: false
